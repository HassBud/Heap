public class Test {

   /* public void bench(priorityQuue queue,int n, int tries, int[] incr){

        int tries = 1000;
        k = 1000;
        min = Double....
        queue.clear
        time start
        for (int i = 0; i < tries.lenght ; i++) {
            for int m = 0; m<n; m++
                    queue.enqueue(m);
            q = dequeue()
            queue.enqueue(q + inkr[j]); // inkr ökar med ertt random ;

        }
        time stop


    }

    main

    int[] incr = new int[k];
    for(int  i < incr lenght)
        incr[i] = rand.nextInt(n)


    bench()*/
}
